# Upgrade checkpoint for Calcit 0.13.77 / Calcit 0.13.77 升级检查点

UTC: 2026-09-04T19:00:21Z

Issue / 任务: [calcit-lang/calcit#740](https://github.com/calcit-lang/calcit/issues/740)

Agent / 身份: `codex-01a06d2e`

Status / 状态: resumed after dependency releases; migration and full web build pass. / 依赖发布后已恢复；迁移及完整 Web 构建通过。

## Resolution / 解除阻塞

- `calcit-theme` 0.4.4 and the aligned Respo dependency chain are now published and verified by strict Caps. / `calcit-theme` 0.4.4 与对齐后的 Respo 依赖链现已发布，并通过 strict Caps 验证。
- Added nominal `Store`, `ErrorData`, `ErrorFrame`, and `ViewerState` contracts, decoded external maps at their boundaries, migrated deprecated enum matching, and replaced nullable DOM access with explicit browser FFI boundaries. / 新增具名状态与错误数据契约，在边界解码外部 map，迁移废弃枚举匹配，并将可空 DOM 访问替换为显式浏览器 FFI 边界。
- The repository keeps exactly one canonical source Snapshot: `calcit.cirru`; `compact.cirru` remains absent. / 仓库只保留一个规范源码 Snapshot：`calcit.cirru`；`compact.cirru` 保持不存在。
- Zero static preprocessing warnings, zero unresolved dynamic method dispatches, zero deprecated calls, quality baseline enforcement, JavaScript code generation, and the Node 24/Vite production build all pass. / 静态预处理零警告、动态方法分派零项、废弃调用零项、质量基线校验、JavaScript 生成及 Node 24/Vite 生产构建均通过。

## Migration / 迁移

- Read the full Calcit agent editing guide with the exact 0.13.77 toolchain before Snapshot changes. / 在修改 Snapshot 前，使用精确的 0.13.77 工具链阅读了完整的 Calcit agent 编辑指南。
- Promoted the last runnable `compact.cirru` snapshot to canonical `calcit.cirru`; the former editor-tree snapshot remains recoverable from Git history and from `/private/tmp/calcit-error-viewer-legacy-editor-issue-740.cirru` in this work environment. / 将最后可运行的 `compact.cirru` 提升为规范的 `calcit.cirru`；旧 editor-tree snapshot 仍可从 Git 历史及当前工作环境的临时备份恢复。
- Migrated only through supported CLIs in stages: 0.13.50 converted legacy top-level configs to `entries.default`, 0.13.51 confirmed the intermediate schema, and 0.13.77 produced the canonical snapshot. The entry mode was explicitly preserved as JavaScript. / 仅通过受支持的 CLI 分阶段迁移：0.13.50 转换旧顶层配置，0.13.51 确认中间 schema，0.13.77 生成规范 snapshot，并明确保持 JavaScript entry mode。
- Set compiler and `@calcit/procs` exactly to 0.13.77, restored package version 0.0.1, replaced mutable dependency branches with current immutable releases, and migrated the lockfile to Yarn 4.12.0. / 将 compiler 与 `@calcit/procs` 精确固定为 0.13.77，恢复包版本 0.0.1，用当前不可变 release 替代分支依赖，并将 lockfile 迁移到 Yarn 4.12.0。
- Modernized CI with Node 24, Corepack, an immutable setup-calcit action pin, least-privilege permissions, immutable Yarn, strict Caps, exact toolchain checks, canonical-format checks, check-only, JS generation, Vite build, and push-only deployment. / CI 已更新为 Node 24、Corepack、不可变 setup-calcit action pin、最小权限、immutable Yarn、strict Caps、精确工具链检查、规范格式检查、check-only、JS 生成、Vite 构建及仅 push 部署。

## Passing evidence / 已通过证据

- Exact Calcit 0.13.77 canonical formatting is idempotent. / 精确 Calcit 0.13.77 的规范格式化具有幂等性。
- `yarn install --immutable` passes with Yarn 4.12.0. / Yarn 4.12.0 的 immutable 安装通过。
- `package.json` and the workflow parse successfully. / `package.json` 与 workflow 解析成功。
- Non-strict dependency installation and exact toolchain verification pass for all resolved project links. / 非严格依赖安装及所有已解析项目链接的精确工具链验证通过。

## Historical blocking evidence (resolved) / 历史阻塞证据（已解决）

At the time of the original checkpoint, the latest published `Cirru/calcit-theme.calcit` release was 0.4.3 at commit `c2a58a46fd5a937b8e5bf4ee064fde95ba8a1acd`, declaring Calcit 0.13.29. Strict Caps then rejected five historical version divergences introduced by that release: `reel` 0.6.6 versus 0.6.12, `respo-ui` 0.7.7 versus 0.7.15, `respo` 0.16.70 versus 0.16.90, `lilac` 0.5.2 versus 0.5.8, and `memof` 0.0.26 versus 0.0.29. This evidence is retained only to explain the former checkpoint; `calcit-theme` 0.4.4 now resolves it. / 在原始 checkpoint 时，最新发布的 `calcit-theme` 是 0.4.3，仍声明 Calcit 0.13.29，并引入上述五项历史版本分歧，因此 strict Caps 当时无法通过。该证据仅用于解释旧 checkpoint；现已由 `calcit-theme` 0.4.4 解决。

With the non-strict graph installed, exact Calcit 0.13.77 `--check-only` stops while loading `calcit-theme.comp.container/slurp`: the published snapshot retains a legacy runtime `Fn` schema where `defmacro` now requires the strict `Macro` contract. This is tracked upstream in [calcit-lang/calcit#746](https://github.com/calcit-lang/calcit/issues/746). / 在安装非严格依赖图后，精确 Calcit 0.13.77 的 `--check-only` 在加载 `calcit-theme.comp.container/slurp` 时停止：发布版 snapshot 仍保留旧 runtime `Fn` schema，而 `defmacro` 现在要求严格 `Macro` contract。上游跟踪见 #746。

At that time, the full entry checks, static quality baseline, JS generation, and Vite build could not be considered valid until a compatible immutable theme release existed. That release is now available as `calcit-theme` 0.4.4, and every listed gate has passed as recorded in Resolution above. The separately known archived Lilac history remains tracked in [calcit-lang/calcit#682](https://github.com/calcit-lang/calcit/issues/682), but it no longer blocks this project. / 当时，在兼容且不可变的 theme release 发布前，完整 entry 检查、静态质量基线、JS 生成及 Vite 构建不能视为有效。现在 `calcit-theme` 0.4.4 已发布，Resolution 中列出的门禁均已通过；Lilac 的归档历史仍由 #682 跟踪，但不再阻塞本项目。

## Former resume condition (satisfied) / 原恢复条件（已满足）

This condition has been satisfied: the compatible immutable releases were adopted, strict Caps and every local/CI gate were rerun successfully, and the focused downstream PR is now open and monitored. / 该条件已经满足：兼容的不可变 release 已采用，strict Caps 与全部本地/CI gate 已成功重跑，聚焦的下游 PR 已创建并持续巡检。
